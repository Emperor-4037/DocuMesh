name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/ai-platform

jobs:
  # ─────────────── Lint ───────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff linter
        run: ruff check gateway/ shared/ services/ scripts/ tests/

  # ─────────────── Unit & Integration Tests ───────────────
  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r gateway/requirements.txt
          pip install pytest httpx pydantic-settings python-json-logger

      - name: Run unit tests
        run: PYTHONPATH=. pytest tests/test_unit.py -v --tb=short

      - name: Run gateway tests
        run: PYTHONPATH=. pytest tests/test_gateway.py -v --tb=short

  # ─────────────── Build & Push Images ───────────────
  build-and-push:
    name: Build & Push Docker Images
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service:
          - name: gateway
            dockerfile: infra/docker/Dockerfile.gateway
          - name: paraphrase-service
            dockerfile: infra/docker/Dockerfile.service
            build_arg: SERVICE_NAME=paraphrase-service
          - name: grammar-service
            dockerfile: infra/docker/Dockerfile.service
            build_arg: SERVICE_NAME=grammar-service
          - name: simplify-service
            dockerfile: infra/docker/Dockerfile.service
            build_arg: SERVICE_NAME=simplify-service
          - name: tone-service
            dockerfile: infra/docker/Dockerfile.service
            build_arg: SERVICE_NAME=tone-service
          - name: summarize-service
            dockerfile: infra/docker/Dockerfile.service
            build_arg: SERVICE_NAME=summarize-service
          - name: rag-service
            dockerfile: infra/docker/Dockerfile.service
            build_arg: SERVICE_NAME=rag-service

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.service.name }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.service.dockerfile }}
          build-args: ${{ matrix.service.build_arg || '' }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ matrix.service.name }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ matrix.service.name }}:${{ github.sha }}

  # ─────────────── Container Security Scan ───────────────
  security-scan:
    name: Trivy Security Scan
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/gateway:latest"
          format: "table"
          exit-code: "1"
          severity: "CRITICAL,HIGH"
