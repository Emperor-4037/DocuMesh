# API Gateway URL (in Docker Compose this is http://gateway:8000, locally use http://localhost:8000)
VITE_API_BASE=http://localhost:8000

# A JWT token for dev. In production, replace with real auth flow.
VITE_DEMO_TOKEN=demo-token
